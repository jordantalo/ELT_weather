{
  "name": "Weather Data Dev",
  "build": {
    "dockerfile": "../deploy/Dockerfile",
    "context": ".."
  },
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "postCreateCommand": "apt-get update && apt-get install -y unzip && curl -LO https://github.com/duckdb/duckdb/releases/download/v1.2.0/duckdb_cli-linux-amd64.zip && unzip -o duckdb_cli-linux-amd64.zip -d /usr/local/bin/ && rm duckdb_cli-linux-amd64.zip",
    "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "innoverio.vscode-dbt-power-user"
      ]
    }
  }
}
